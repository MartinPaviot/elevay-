# 29 — skills (`/skills`) — audit d'hydratation

**Verdict global : H4 (non câblé).** The custom-skills lanes (Workspace, Personal) are genuinely H1: real tenant- and user-scoped DB reads from customSkillTemplates with eq(tenantId), a written 'No skills yet' empty state, and a page-level loading spinner. The System skills lane — which is the bulk of the page and the sole content of the Explore tab — is effectively H4: it depends on the in-memory SKILL_REGISTRY Map being populated by registerAllSkills(), but that function is only ever called from a DIFFERENT route (/api/skills/[slug]), never from the GET /api/settings/skills handler this page calls. So on a cold server the System section renders empty and Explore shows 'No skills available'; population is a non-deterministic side-effect of whether the other route warmed the process first. Additionally, even when system skills do appear, the GET route maps them with only 9 fields (hasSteps:false, no steps/constraints/parameters/guidelines/cost, useCount:0), so the detail panel for any system skill is structurally thin (H2).

Entrée : `app/apps/web/src/app/(dashboard)/skills/page.tsx`.

## Éléments

| Élément | file:line | Source (file:line) | État | Tenant | Loading | Empty | Error | Fresh | Note |
|---------|-----------|--------------------|------|--------|---------|-------|-------|-------|------|
| Header title + Wand2 icon ('Skills') | app/apps/web/src/app/(dashboard)/skills/page.tsx:99-105 | static label | H0 | n/a | n/a | n/a | n/a | static | Pure chrome, correctly static. |
| List/Explore view toggle + Create skill button | app/apps/web/src/app/(dashboard)/skills/page.tsx:110-148 | client state (viewMode), static labels | H0 | n/a | n/a | n/a | n/a | static | Chrome/controls, faithful. |
| System skills section (sidebar + count badge + Explore grid) | app/apps/web/src/components/skills/skill-sidebar.tsx:133-140; explore-grid.tsx:64-165 | GET /api/settings/skills -> listSkills() from in-memory SKILL_REGISTRY (route.ts:19-30) | H4 | n/a | spinner | handled | silent | once | DEFECT: the registry Map is only populated by registerAllSkills(), which is called ONLY in /api/skills/[slug]/route.ts (route.ts:8) — NEVER in the GET /api/settings/skills handler that this page calls (settings/skills/route.ts:5,19 imports listSkills but not register-all). On a cold/separate server process, listSkills() returns [], so the System section and the entire Explore tab (which renders only systemSkills, page.tsx:191) show empty. Population is a non-deterministic race depending on whether the [slug] route was hit first in the same warm instance. Not reliably wired. |
| Workspace skills section (sidebar list + count badge) | app/apps/web/src/components/skills/skill-sidebar.tsx:147-154 | listAvailableSkills() -> customSkillTemplates WHERE tenantId + scope='workspace' (executor.ts:294-311) | H1 | yes | spinner | handled | silent | once | Faithful: real tenant-scoped DB data (eq(tenantId)), 'No skills yet' empty state (skill-sidebar.tsx:63-67), loading spinner on page. |
| Personal skills section (sidebar list + count badge) | app/apps/web/src/components/skills/skill-sidebar.tsx:161-168 | listAvailableSkills() -> customSkillTemplates WHERE tenantId + scope='user' + createdByUserId=userId (executor.ts:300-308,319) | H1 | yes | spinner | handled | silent | once | Faithful: tenant + user scoped DB data; 'No skills yet' empty state; loading spinner. |
| Skill detail: name / category badge / scope / description | app/apps/web/src/components/skills/skill-detail.tsx:62-137 | selected SkillEntry from GET /api/settings/skills (custom from DB, system from registry) | H1 | yes | none | handled | n/a | once | For custom skills these are real tenant-scoped DB fields; for system skills they are real registry constants. Faithful for what is shown. |
| Skill detail: Steps / Constraints / Parameters / Guidelines / Cost estimate sections | app/apps/web/src/components/skills/skill-detail.tsx:139-266 | SkillEntry.steps/constraints/parameters/guidelines/costEstimate | H2 | yes | none | handled | n/a | once | For custom skills these can be populated; but the GET route maps SYSTEM skills with only 9 fields and hasSteps:false (settings/skills/route.ts:19-30) — it never returns steps/constraints/parameters/guidelines/costEstimate. So selecting any system skill shows a detail panel with NO steps/constraints/params even though the registry SkillDefinition has them. Partial: real where present, structurally missing for system skills. |
| Skill detail: usage stats (Used N times / Last used date) | app/apps/web/src/components/skills/skill-detail.tsx:268-310 | SkillEntry.useCount / lastUsedAt | H2 | yes | none | handled | n/a | once | Custom skills get real useCount/lastUsedAt incremented on execution (executor.ts:170-178, 320-321) and the block self-hides when zero. But system skills are hardcoded useCount:0/lastUsedAt:null in the route (settings/skills/route.ts:26-28), so usage is never tracked/shown for system skills — block silently hides. Partial. |
| Empty-selection placeholder ('Select a skill to view details') | app/apps/web/src/app/(dashboard)/skills/page.tsx:176-184 | static (no selection) | H0 | n/a | n/a | handled | n/a | static | Correct empty/idle chrome. |

## Pires défauts

1. System skills are unreliable/unwired: GET /api/settings/skills calls listSkills() (settings/skills/route.ts:19) against an in-memory Map that is only populated by registerAllSkills() called exclusively in api/skills/[slug]/route.ts:8 — never in this route. Cold process => System section + entire Explore tab render empty (registry.ts:15-17).
2. System skill detail is structurally incomplete: the route maps system skills with hasSteps:false and omits steps/constraints/parameters/guidelines/costEstimate (settings/skills/route.ts:19-30), so SkillDetail never shows those sections for system skills despite the registry SkillDefinition containing them (skill-detail.tsx:160-266).
3. System skill usage stats are hardcoded useCount:0 / lastUsedAt:null (settings/skills/route.ts:26-28); usage is only ever incremented for custom skills (executor.ts:170-178), so the usage block silently hides for all system skills rather than reflecting real run counts.
