# 33 — cs-today (`/cs/today`) — audit d'hydratation

**Verdict global : H4 (non câblé).** The /cs/today daily priority queue is mostly faithfully hydrated: a single client fetch to a properly tenant-scoped route (eq tenant_id on both the latest-snapshot subquery and the companies join) feeds real health scores, component breakdowns, and risk levels computed from live activities/deals by a registered daily Inngest cron, with real loading/empty/error states matching the Home bar. The page's worst defect is the ARR exposure: the column exists in schema and is consumed by both the badge and the route's sort tie-breaker, but the cron insert never writes arrExposureUsd, so it is permanently null — the badge never shows and the header's promised 'risk × ARR' ranking is dead (H4). Secondary issues: the 'AI suggested action' is a static template (data-driven axis selection but never the promised LLM enrichment), and the queue can be silently up to 24h stale with no freshness indicator.

Entrée : `app/apps/web/src/app/(dashboard)/cs/today/page.tsx`.

## Éléments

| Élément | file:line | Source (file:line) | État | Tenant | Loading | Empty | Error | Fresh | Note |
|---------|-----------|--------------------|------|--------|---------|-------|-------|-------|------|
| Page header title/subtitle ("Today" / "ranked by risk × ARR") | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:85-90 | static (hardcoded copy) | H0 | n/a | n/a | n/a | n/a | static | Pure chrome label. BUT the subtitle promises 'risk × ARR' ranking that is not actually achieved because ARR is never populated (see ARR badge) — copy overstates the live behavior. |
| Account name + avatar initials (per card) | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:155,190,195-197 | GET /api/cs/today → companies.name/domain joined on snapshot (route.ts:64-66,80-81) | H1 | yes | skeleton | handled | global | poll | faithful — real tenant-scoped company name, falls back to domain then 'Unknown account'. |
| Risk pill (ConfidenceState) + Health x/100 | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:198-202 | snapshot.risk_level / health_score from computeHealthScore over real activities+deals (cs-health-cron.ts:181-200) | H1 | yes | skeleton | handled | global | poll | faithful — risk_level + healthScore are computed from real DB signals and stored per tenant/account. |
| ARR exposure badge ($...) | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:156-158,203-213 | snapshot.arr_exposure_usd (route.ts:91-94) — column exists but cron NEVER writes it (cs-health-cron.ts:192-201 insert omits arrExposureUsd) | H4 | yes | none | blank | global | static | Unwired. arrExposureUsd is in the schema (cs.ts:52) and read by both the page badge and the route's sort tie-breaker, but the cron insert never sets it, so it is always null → badge never renders and 'rank by ARR' is dead. Defect. |
| Component breakdown chips (usage/sentiment/engagement/velocity/support) | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:217-231 | snapshot.components jsonb from computeHealthScore (health-score.ts:93-116; inputs from activities/deals in cs-health-cron.ts:47-149) | H1 | yes | skeleton | handled | global | poll | faithful — each axis is computed from real tenant activity/deal data and persisted; color thresholds applied client-side. |
| Suggested next action + reason | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:234-253 | snapshot.suggested_action / reason = defaultNextActionFor(weakestAxis) (health-score.ts:125-155, written cron.ts:198-199) | H2 | yes | skeleton | handled | global | poll | Real-data-DRIVEN selection (weakest axis is computed from live data) but the action/reason STRINGS are static templates — the code comments admit the LLM enrichment is a 'future sprint' and never happens. Acceptable as data-driven copy, but not genuinely generated/grounded as the H0 doc-comment claims. |
| Relative computed-at timestamp (per card) | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:47-54,261 | snapshot.computed_at (route.ts:95-98) | H2 | yes | skeleton | handled | global | once | Real value, but the cron only runs daily at 04:00 UTC (and does not run under local pnpm dev per project notes); list can be up to 24h stale with no staleness/refresh-needed indicator beyond the relative-time text. Freshness is poll-on-mount only. |
| Empty state ('No health snapshots yet') | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:123-139 | items.length === 0 from route response | H1 | n/a | n/a | handled | n/a | n/a | faithful written empty state explaining the daily cron — matches the Home reference bar. |
| Error banner | app/apps/web/src/app/(dashboard)/cs/today/page.tsx:109-121 | fetch catch (page.tsx:69-71) | H1 | n/a | n/a | n/a | global | n/a | role=alert error degradation present; but it is a single global error for the whole list (no independent per-lane degradation, since the page is a single list — acceptable for a single-source page). |

## Pires défauts

1. ARR exposure is unwired end-to-end: the cron never sets arrExposureUsd (cs-health-cron.ts:192-201 insert omits it) yet the page badge (page.tsx:156-158,203-213) and the route sort tie-breaker (route.ts:91-94,105) both depend on it — badge always hidden and the advertised 'rank by ARR' ordering never takes effect.
2. Header copy overstates behavior: subtitle says accounts are 'ranked by risk × ARR' (page.tsx:89) but ARR is always null, so ranking degenerates to risk-then-healthScore only (route.ts:101-109) — misleading.
3. Stale-without-signal: snapshots come from a once-daily 04:00 UTC cron that does not run under local dev (project note), and the list polls only on mount/refresh with no max-age/staleness indicator (computed_at relative text at page.tsx:261 is the only hint) — queue can silently lag real account state by ~24h.
