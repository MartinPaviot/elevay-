# Benchmark — evidence

Text analyses are versioned here (`competitors-matrix.md` + `raw/*.md`).

The RAW evidence — 66 saved competitor HTML pages + 69 screenshots (~130 MB) —
is deliberately NOT in git (repo convention: research binaries stay out of
version control). It lives in the working tree under `benchmark/html/` and the
screenshot files; re-capture via the Phase 0 Playwright scripts if needed.
